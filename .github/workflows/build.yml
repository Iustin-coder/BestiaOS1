name: Compilare BestiaOS Ultimate Failsafe Active

on:
  workflow_dispatch: # Pornire manuala din interfata

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - name: Eliberare Spatiu Brut (Deblocare Maxima ~100GB)
      run: |
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift /usr/local/share/chromium
        sudo docker image prune --all --force
        sudo apt-get clean

    - name: Instalare Dependente Compilare Linux
      run: |
        sudo apt-get update
        sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git gnupg gperf lib32ncurses5-dev lib32readline-dev lib32z1-dev liblz4-tool libncurses5 libncurses5-dev libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev python3
        mkdir -p ~/bin
        curl -s https://googlesource.com | base64 --decode > ~/bin/repo
        chmod a+x ~/bin/repo
        export PATH=~/bin:$PATH

    - name: Setare Identitate Git
      run: |
        git config --global user.name "BestiaOS Core Developer"
        git config --global user.email "developer@bestiaos.world"

    - name: Sincronizare Automata Android si Componente Raphael
      run: |
        mkdir android-source && cd android-source
        export PATH=~/bin:$PATH
        
        # Initializam codul de baza pe ramura ACTIVA (lineage-20.0 - Android 13)
        repo init -u https://github.com -b lineage-20.0 --depth=1 --clone-bundle
        
        # Injectam tabelul XML cu ramurile active corecte pentru Redmi K20 Pro Premium
        mkdir -p .repo/local_manifests
        cat <<EOF > .repo/local_manifests/raphael.xml
        <?xml version="1.0" encoding="UTF-8"?>
        <manifest>
          <project name="LineageOS/android_device_xiaomi_raphael" path="device/xiaomi/raphael" remote="github" revision="lineage-20.0" />
          <project name="LineageOS/android_kernel_xiaomi_sm8150" path="kernel/xiaomi/sm8150" remote="github" revision="lineage-20.0" />
          <project name="TheMuppets/proprietary_vendor_xiaomi_raphael" path="vendor/xiaomi/raphael" remote="github" revision="lineage-20.0" />
        </manifest>
        EOF
        
        # Descarcam datele reale de pe servere
        repo sync -c -j$(nproc --all) --force-sync --no-clone-bundle --no-tags --prune

    - name: Aplicare Branding BestiaOS si Optimizari 12GB RAM
      run: |
        cd android-source
        
        # Verificare stricta: Daca descarcarea a esuat din nou, oprim procesul cu explicatie clara
        if [ ! -d "vendor" ]; then
            echo "Eroare critica: Serverele externe au blocat conexiunea. Folderul vendor lipseste." && exit 1
        fi
        
        CONFIG_PATH=$(find vendor/ -name "common.mk" | head -n 1)
        if [ ! -z "$CONFIG_PATH" ]; then
            DIR_BRANDING=$(dirname "$CONFIG_PATH")
            find "$DIR_BRANDING" -type f -name "*.mk" -exec sed -i 's/LineageOS/BestiaOS/g' {} +
            
            echo "PRODUCT_PROPERTY_OVERRIDES += ro.build.display.id=BestiaOS_v1.0-Premium" >> "$CONFIG_PATH"
            echo "PRODUCT_PROPERTY_OVERRIDES += ro.bestia.version=1.0-Ultimate" >> "$CONFIG_PATH"
            echo "PRODUCT_PROPERTY_OVERRIDES += ro.vendor.qti.sys.fw.bg_apps_limit=60" >> "$CONFIG_PATH"
            echo "PRODUCT_PROPERTY_OVERRIDES += dalvik.vm.heapsize=512m" >> "$CONFIG_PATH"
        fi

    - name: Executare Compilare Sistem (mka bacon)
      run: |
        cd android-source
        export PATH=~/bin:$PATH
        
        # Daca mediul de build nu s-a descarcat, oprim executia curat
        if [ ! -f "build/envsetup.sh" ]; then
            echo "Eroare critica: Fisierele de compilare esentiale nu au fost gasite." && exit 1
        fi
        
        source build/envsetup.sh
        lunch lineage_raphael-userdebug
        mka bacon -j$(nproc --all)

    - name: Livrare Fisier Zip Gata de Instalat
      uses: actions/upload-artifact@v4
      with:
        name: BestiaOS-v1.0-RedmiK20ProPremium
        path: android-source/out/target/product/raphael/*.zip
